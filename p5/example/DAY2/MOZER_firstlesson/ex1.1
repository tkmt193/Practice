// 全体の初期化（最初に一回だけ呼ばれる）
function setup() {
  // キャンバスをつくる
  createCanvas(500, 500);
}

// 計算と表示
function draw() {
  // 背景をぬりつぶす
  background(0, 0, 0);

  // ぬりつぶす
  fill(255, 255, 0);

  // 円を描く
  ellipse(250, 250, 300);
}
